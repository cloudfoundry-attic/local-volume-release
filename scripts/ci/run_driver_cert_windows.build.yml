----
platform: windows

inputs:
- name: local-volume-release

run:
  path: powershell
  args:
    - "-ExecutionPolicy"
    - "ByPass"
    - "-File"
    - "local-volume-release/scripts/ci/run_driver_cert.ps1"
