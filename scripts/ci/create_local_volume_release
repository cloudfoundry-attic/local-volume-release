#!/bin/bash
#run_deploy

set -x -e

version=`cat local-volume-version/number`
tarball_name=local-volume-$version.tgz

pushd local-volume-release
  bosh -n create-release --version=$version --force --tarball=$tarball_name
  mv $tarball_name ../created-local-volume-release
popd
